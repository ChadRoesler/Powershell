{
    "Resources" : {
        "#{TargetGroup}" : {
            "Type" : "AWS::ElasticLoadBalancingV2::Listener",
            "Properties" : {
                "Port" : #{PortNumber},
                "Protocol" : #{Protocol}

            }
        }
    }
}